import React from 'react';
import Fade from 'react-reveal/Fade';

const Descriptions = () => {
    return(
        <Fade>
            <div className="center_wrapper">
                <h2>HighLights</h2>
                <div className="highlight_description">
                    Useful plugin when you're putting templates together and alike. However only being able to replace text with latin as opposed to generating x amount of latin makes for a more complicated workflow in most cases.
                </div>
            </div>
        </Fade>
    );
};

export default Descriptions;